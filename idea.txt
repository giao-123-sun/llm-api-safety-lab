现在有很多网站让你放一个api key， baseurl,model来提供模型API来进行运行，这样的好处是作者不必开源所有东西，也不用承担每次运行服务的昂贵费用，但是这种方式后面是否会造成一些攻击？会不会用户使用的并非单纯的模型，而是伪装的攻击程序、智能体；这个情况会不会导致网站服务被攻破，自主攻破；以及是否会由智能体进行逆向，来盗取这个算法或者程序本身。现在很多只是用模型prompt，但是随着后面的demo上升到今年的agent自主程序和其他的东西，那些被给予沙盒环境和MCP，skill调用权限的模型，是否会导致这种问题？这个对后面的LLM API safety有什么影响。